packages:
  - widget

# Versions shared by the widget's rollup build (widget/rollup.config.js, the
# infoview bundle), pinned once here rather than inline so a later workspace
# member can reuse the same resolutions.
catalog:
  '@rollup/plugin-commonjs': ^28.0.1
  '@rollup/plugin-node-resolve': ^15.0.2
  '@rollup/plugin-replace': ^6.0.1
  react: ^18.2.0
  react-dom: ^18.2.0
  rollup: ^4.17.2
